---
/* =============================================================
   Pricing — Basic / Pro / Premium cards. The "popular" plan is
   highlighted with the brand gradient border + badge.
   ============================================================= */
import SectionHeader from "./SectionHeader.astro"
import { getLangFromUrl, useTranslations, useTranslatedPath } from "@i18n/utils"
import { pricing } from "@data/content"

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)
const tp = useTranslatedPath(lang)
---

<section class="section relative" id="pricing">
  <div class="container-x">
    <SectionHeader
      eyebrow={t("pricing.title")}
      title={t("pricing.title")}
      subtitle={t("pricing.subtitle")}
    />

    <div class="mt-14 grid items-stretch gap-6 lg:grid-cols-3">
      {
        pricing.map((plan, i) => (
          <article
            class:list={[
              "reveal relative flex flex-col rounded-3xl border p-8 transition-all duration-300",
              plan.popular
                ? "border-transparent bg-surface shadow-glow lg:-translate-y-4"
                : "border-border bg-surface hover:-translate-y-1 hover:border-brand-pink/50",
            ]}
            data-reveal-delay={i * 0.08}
          >
            {plan.popular && (
              <>
                <span
                  class="pointer-events-none absolute inset-0 -z-10 rounded-3xl bg-brand-gradient p-px"
                  aria-hidden="true"
                >
                  <span class="block h-full w-full rounded-3xl bg-surface" />
                </span>
                <span class="absolute -top-3 start-1/2 -translate-x-1/2 rounded-full bg-brand-gradient px-4 py-1 text-xs font-bold text-white shadow-glow">
                  {t("pricing.popular")}
                </span>
              </>
            )}

            <h3 class="text-lg font-bold text-fg-muted">{plan.name[lang]}</h3>
            <div class="mt-3 flex items-baseline gap-1">
              <span class="text-3xl font-extrabold text-gradient">
                {plan.price[lang]}
              </span>
            </div>

            <ul class="mt-6 flex-1 space-y-3">
              {plan.features[lang].map((f) => (
                <li class="flex items-start gap-3 text-fg-muted">
                  <svg viewBox="0 0 24 24" class="mt-1 h-4 w-4 flex-none text-brand-pink" fill="none" stroke="currentColor" stroke-width="3">
                    <path d="M20 6 9 17l-5-5" stroke-linecap="round" stroke-linejoin="round" />
                  </svg>
                  <span>{f}</span>
                </li>
              ))}
            </ul>

            <a
              href={tp("/contact")}
              class:list={[
                "mt-8 text-center",
                plan.popular ? "btn-primary" : "btn-ghost",
              ]}
            >
              {t("pricing.cta")}
            </a>
          </article>
        ))
      }
    </div>
  </div>
</section>
